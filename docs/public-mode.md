# Public Mode

Set `visibility.mode` to `public`, `operator`, or `private` in config.

- `public`: intended for live.meshcore.ca, hide operator tooling and sensitive fields.
- `operator`: full operational dashboards.
- `private`: internal-only deployment profile.

Also set `.env` `PUBLIC_MODE=public` for production profile wiring.
