# Operator runbook

- Validate config profile selection with `CORESCOPE_CONFIG_PROFILE`.
- Keep credentials in environment variables only.
- Verify public mode behavior before exposing a public URL.
- Run unit/integration tests before release.
